
  # Node-based Workflow UI Design

  This is a code bundle for Node-based Workflow UI Design. The original project is available at https://www.figma.com/design/VUmV7lzoEmapXUiajFm4gx/Node-based-Workflow-UI-Design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  